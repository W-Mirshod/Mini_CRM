version: '3.8'

services:
  mini-crm:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    environment:
      - DATABASE_URL=sqlite+aiosqlite:///./mini_crm.db
    networks:
      - web

networks:
  web:
    external: true
